{
  "scripts": {
    "clean": "rimraf ./.tmp/dist",
    "build:clean": "npm run clean && npm run build",
    "dev": "rollup -c -w",
    "build": "rollup -c"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^8.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.1",
  }
}
